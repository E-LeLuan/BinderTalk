install.packages("ggplot2")
install.packages("dplyr")

#This is not nessecary for the script example as it only requires base R.
#However, this is how you would add any packages your script requires.
